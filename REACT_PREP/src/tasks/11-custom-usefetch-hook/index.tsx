import './styles.css';

export default function UseFetchDemo() {
  // TODO: const { data, loading, error } = useFetch<SomeType>('/api/some-endpoint'); (see ./useFetch.ts)

  return <div className="use-fetch-demo">{/* TODO: render loading / error / data states */}</div>;
}
